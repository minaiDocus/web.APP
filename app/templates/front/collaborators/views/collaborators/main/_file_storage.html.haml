- if @user.is_admin && @organization.is_active
  #file_storages.tab-pane.fade{ class: ('active show' if params[:tab] == 'file_storages') }
    = render partial: 'account/customers/file_storages', locals: { user: @member.user }
    / TODO ////
    = link_to t('actions.edit'), file_storage_authorizations_edit_path(@organization, @member), class: 'btn btn-primary'
