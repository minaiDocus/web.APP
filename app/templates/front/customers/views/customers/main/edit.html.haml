#customer.derivationRight
  .card
    .card-header
      %h3
        Edition du client : 
        = link_to @customer, organization_customer_path(@organization, @customer), class: 'fs-origin'
    = render partial: 'form'