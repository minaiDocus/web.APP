/ #groups
/   .row.derivationRight
/     .col-md-12
/       .box.clearfix
/         .float-left
/           %h3
/             Liste des groupes 
/             %span.badge.badge-info= @groups.total_count
/         .float-right
/           - if @user.leader? && @organization.is_active
/             = link_to icon_new + ' ' + t('actions.new'), new_organization_group_path(@organization), class: 'btn btn-primary'

/   .row.retractable.with-filter.derivationLeft
/     .col-md-12
/       .box
/         = render partial: 'shared/list_options', locals: { collection: @groups }
/         %table.table.table-condensed.table-striped.margin1top.margin1bottom.table-detachable-head.table-bordered-inner
/           %thead
/             %tr.bg-brown.text-white
/               %th= sortable :created_at, t('activerecord.models.common.attributes.created_at')
/               %th= sortable :name, t('activerecord.models.group.attributes.name')
/               %th= t('activerecord.models.group.attributes.collaborators_count')
/               %th= t('activerecord.models.group.attributes.customers_count')
/               %th= t('actions.name')
/           %tbody.retractable.slidedown
/             - @groups.each do |group|
/               %tr
/                 %td= l(group.created_at)
/                 %td= link_to group.name, organization_group_path(@organization, group)
/                 %td= group.collaborators.count
/                 %td= group.customers.count
/                 %td
/                   - if @organization.is_active
/                     - if @user.leader? || @user.manage_groups
/                       = link_to 'Éditer', edit_organization_group_path(@organization, group), class: 'btn btn-light'
/                     - if @user.leader?
/                       - if group.is_dropbox_authorized
/                         - confirm = "Attention ! la suppression de ce groupe entraîne l'arrêt du déversement automatique de ses documents dans votre dropbox. Voulez-vous continuer ?"
/                       - else
/                         - confirm = 'Etês vous sûr ?'
/                       = link_to 'supprimer', organization_group_path(@organization, group), class: 'btn btn-light', data: { method: 'DELETE', confirm: confirm }
/         = render partial: 'shared/list_options', locals: { collection: @groups }
/     = render partial: 'sidebar'
- @groups.each_slice(3) do |groups|
  .row.mt-3
    - groups.each do |group|
      .col-md-4.box.no-margin.m-2.padding-group-collaborator
        .head.row
          .col-md-11
            .title.semibold= link_to group.name, organization_group_path(@organization, group)
            .content-box= group.created_at.strftime('%d%m%Y')
          .col-md-1
            .action.edit_group.pointer.list-groupe
              %span.glyph= glyphicon('ellipses', { size: '15', color: 'rgba(26, 26, 26, 0.7);'})
            .sub_menu.text-start.hide
              %ul.no-margin.no-padding.regular.edit-delete{id: "group-#{group.id}"}
                - if @organization.is_active
                  - if @user.leader? || @user.manage_groups
                    %li.edit.member-group.add-or-edit
                      %span= glyphicon('pencil', { size: '15', color: 'rgba(26, 26, 26, 0.7);'})
                      &nbsp;Éditer
                  - if @user.leader?
                    - if group.is_dropbox_authorized
                      - confirm = "Attention ! la suppression de ce groupe entraîne l'arrêt du déversement automatique de ses documents dans votre dropbox. Voulez-vous continuer ?"
                    - else
                      - confirm = 'Etês vous sûr ?'
                    %li.delete.red-label
                      = link_to glyphicon('trash', { size: '15', color: "#FF4848" }) + ' supprimer', organization_group_path(@organization, group), data: { method: 'DELETE', confirm: confirm }
        %br        
        .body.content-box
          Le groupe direction se compose des administrateurs
          %br
          et d’autres personnes vraiment gentilles.

          .row.mt-3
            .col-md-11
              .semibold Nombre de collaborateurs :
            .col-md-1
              %span.badge.badge-secondary.badge-sm.semibold= group.collaborators.count
          .row.mt-2
            .col-md-11
              .semibold Nombre de clients :
            .col-md-1
              %span.badge.badge-secondary.badge-sm.semibold= group.customers.count


.modal#edit-group
  .modal-dialog
    .modal-content