- if period
  .organization_quota
    %span= format_price_00(period.organization_basic_excesses_price.to_f + period.organization_plus_micro_excesses_price.to_f) + " €"
    .details.box.hide
      %table.table.table-condensed
        %thead
          %tr
            %th Dépassement
            %th.center Basiques
            %th.center Micro
        %tbody
          %tr
            %td Pré-affectation
            %td.center= period.basic_excess
            %td.center= period.plus_micro_excess
- else
  = '-'