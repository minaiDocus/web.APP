= stylesheet_link_tag 'front/rights/assets/stylesheets/main'

#collaborator_rights
  .card
    .card-header
      %h3
        Edition des autorisations du collaborateur : 
        = link_to @member.info, collaborators_show_path(@organization, @member, tab: 'authorization'), class: 'fs-origin'
    = simple_form_for @member, url: rights_update_path(@organization, @member), html: { class: 'form form-horizontal margin0' } do |f|
      .card-body
        = f.input :manage_collaborators,     as: :boolean
        = f.input :manage_customers,         as: :boolean
        = f.input :manage_groups,            as: :boolean
        = f.input :manage_journals,          as: :boolean
        = f.input :manage_customer_journals, as: :boolean
      .card-footer
        .form-actions
          = f.submit t('actions.submit'), class: 'btn btn-primary'
          = link_to t('actions.cancel'), collaborators_show_path(@organization, @member, tab: 'authorization'), class: 'btn'

= stylesheet_link_tag 'front/rights/assets/javascripts/main'