!!!
%html
  %head
    %title
      - if request.env["SERVER_NAME"].include?("axelium")
        Axelium | Connexion
      - elsif request.env["SERVER_NAME"].include?("dkpartners")
        DK Partners | Connexion
      - elsif request.env["SERVER_NAME"].include?("censial")
        Censial Online | Connexion
      - elsif request.env["SERVER_NAME"].include?("orial")
        My Orial | Connexion
      - else
        Idocus | Connexion

    %meta{ :content => "text/html; charset=UTF-8", "http-equiv" => "Content-type" }
    %meta{ name: "viewport", content: "width=device-width, initial-scale=1" }

    %link{ :rel => 'shortcut icon', :type => "image/ico", :href => image_path('favicon.ico') }
    - if request.env["SERVER_NAME"].include?("axelium")
      = stylesheet_link_tag 'front/main_axelium'
    - elsif request.env["SERVER_NAME"].include?("dkpartners")
      = stylesheet_link_tag 'front/main_dk_partners'
    - elsif request.env["SERVER_NAME"].include?("censial")
      = stylesheet_link_tag 'front/main_censial'
    - elsif request.env["SERVER_NAME"].include?("orial")
      = stylesheet_link_tag 'front/main_orial'
    - else
      = stylesheet_link_tag 'front/main'

  .body_content
    .container-fluid.text-center.login
      .box.text-center.fadein
        %h4.regular.m-3.p-4.semibold
          Déverrouiller mon compte

        %p
          Veuillez saisir votre adresse e-mail.
          Ensuite, vérifiez votre boîte aux lettres, nous vous envoyons immédiatement
          un mail qui vous indiquera la démarche à suivre pour déverrouiller votre compte.
        %br

        = simple_form_for resource, url: unlock_path(resource_name) do |f|
          .row.text-center
            .col-md-12
              .input-group.mb-3
                %span.input-group-text#input-email= glyphicon('envelope-closed', { size: '14', color: 'background: rgba(26, 26, 26, 0.7)' })
                = f.input :email, label: 'Votre adresse e-mail :'
          .row.mt-3
            .col-md-12.text-center
              = f.submit 'Valider', class: 'btn btn-primary float-left'
